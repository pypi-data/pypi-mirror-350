[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qubitconfig"
version = "25.05.0"
dependencies = [
    "numpy",
    ]


# [tool.hatch.build.targets.wheel]
# packages = ["python/distproc"]
