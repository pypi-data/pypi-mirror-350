[project]
name = "flask-field"
version = "0.1.1"
description = "My (not) awesome first project"
readme = "README.md"
requires-python = ">=3.7"
license = "MIT"

authors = [
  { name="bloba2000", email="gitlerrar@gmail.com" }
]

classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/Xannix246/flask-field"

[tool.setuptools]
packages = ["flask-field"]
include-package-data = true
