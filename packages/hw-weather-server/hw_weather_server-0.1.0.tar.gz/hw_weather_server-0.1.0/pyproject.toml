[project]
name = "hw-weather-server"
version = "0.1.0"
description = "search weather by location"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "mcp>=1.9.1",]
[[project.authors]]
name = "wenzw"
email = "wenzw@huaweisoft.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
hw-weather-server = "hw_weather_server:main"
