# pyproject.toml

[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lib-test-package"
version = "1.0.0"
description = "This package is useless"
readme = "README.md"
authors = [{ name = "Python anonym", email = "info@realpython.com" }]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
keywords = ["test"]
dependencies = [
    "pandas>=2.2.0"
]
requires-python = ">=3.12"

[project.scripts]
realpython = "main.__main__:main"