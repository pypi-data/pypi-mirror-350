[project]
name = "fastapi-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "uptick-observability[fastapi]",
    "uvicorn",
    "fastapi"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.2",
]

[tool.uv.sources]
uptick-observability = { workspace = true }
