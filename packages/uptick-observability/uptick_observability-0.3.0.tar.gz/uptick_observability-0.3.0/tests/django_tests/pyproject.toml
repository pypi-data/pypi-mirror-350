[project]
name = "django-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "uptick-observability[django,dramatiq,celery]",
    "django>=5.1",
    "dramatiq>=1.17.0",
    "daphne>=4.1.2",
    "psycopg2>=2.9.9",
    "redis>=5.0.8",
    "django-dramatiq>=0.11.6",
    "celery>=5.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.2",
]

[tool.uv.sources]
uptick-observability = { workspace = true }
