[project]
name = "cllm-cuiq-test"
description = "A simple demo package"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [{ name = "cuiq", email = "cuiq@google.com" }]
keywords = ["llm", "demo", "example", "cuiq"]
dynamic = [ "version"]

[project.urls]
Homepage = "https://github.com/cuiq/cllm/cllm-cuiq-test"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
