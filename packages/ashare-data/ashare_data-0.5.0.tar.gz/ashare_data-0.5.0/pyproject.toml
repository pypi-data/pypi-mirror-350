[project]
name = "ashare-data"
version = "0.5.0"
description = "A command-line tool for A-share market data management."
readme = "README.md"
authors = [
    { name = 'zw', email = 'szzend@qq.com' }
]
requires-python = ">=3.13"
dependencies = [
    "akshare>=1.16.73",
    "click>=8.1.8",
    "pandas>=2.2.3",
    "pyarrow>=19.0.1",
]

[project.scripts]
ashare = "ashare_data.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
