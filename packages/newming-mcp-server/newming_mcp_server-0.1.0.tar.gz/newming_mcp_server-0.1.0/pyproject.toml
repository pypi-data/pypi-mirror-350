[project]
name = "newming-mcp-server"
version = "0.1.0"
description = "뉴밍의 MCP 서버입니다."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.31.0",
    "fastmcp>=1.0.0"
]

[tool.setuptools]
py-modules = ["sse"]