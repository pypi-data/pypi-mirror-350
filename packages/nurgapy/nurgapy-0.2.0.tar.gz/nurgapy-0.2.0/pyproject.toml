[project]
name = "nurgapy"
version = "0.2.0"
description = "This lightweight Python library provides reusable utility functions to simplify common development tasks."
authors = [
    {name = "nurgalive",email = "github@nurgalive.com"}
]
license = {text = "MIT"}
readme = "README.md"
repository = "https://github.com/nurgalive/nurgapy"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pre-commit = "^4.0.1"
pytest = "^8.3.4"
