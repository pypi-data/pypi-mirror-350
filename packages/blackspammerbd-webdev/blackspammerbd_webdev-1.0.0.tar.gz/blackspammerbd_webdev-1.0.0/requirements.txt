click>=8.0             # CLI (command line interface) তৈরি করতে ব্যবহৃত হয়
requests>=2.25         # HTTP রিকুয়েস্ট পাঠাতে (API, ওয়েব পেজ)
beautifulsoup4>=4.9    # HTML পার্সিং-এর জন্য (স্ক্র্যাপিং)
tqdm>=4.64             # প্রোগ্রেস বার দেখানোর জন্য
PyYAML>=6.0            # YAML ফাইল হ্যান্ডেল করতে
colorama>=0.4          # CLI তে রঙ যুক্ত করার জন্য (Windows এ ভালো support দেয়)
# pyppeteer>=1.0.0     # (ঐচ্ছিক) যদি JS রেন্ডার করা দরকার হয়
