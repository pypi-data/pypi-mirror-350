[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "babadb"
version = "1.0.0"
description = "BabaDB — Encrypted Async File-Based Database for Pydantic Models"
readme = "readme.md"
requires-python = ">=3.7"
authors = [
  {name = "noxzion", email = "negroid2281488ilikrilex@gmail.com"}
]
license = {text = "MIT"}
keywords = ["mongodb", "motor", "pydantic", "async", "odm"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent"
]

dependencies = [
    "pydantic>=1.8,<2.0",
]