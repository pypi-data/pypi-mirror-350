[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ksfeatureselector"
version = "0.1.1"
description = "Select features using Kolmogorov–Smirnov (K-S) test for binary classification tasks."
authors = [
    { name="V Subrahmanya Raghu Ram Kishore Parupudi", email="pvsrrkishore@gmail.com" }
]
license = { text = "MIT" }
readme = "README.rst"
requires-python = ">=3.7"
dependencies = [
    "pandas>=1.0.0",
    "scipy>=1.5.0"
]

[tool.setuptools]
packages = ["ksfeatureselector"]
