## YAML Template.
---
version: "2.1"

services:

  vepfetcher:
    container_name: vepfetcher
    image: docker.gitlab.gwdg.de/ukeb/mtb/vus-predict/vepadapter:develop
    expose:
      - "9006"
    ports:
      - "9006:9006"
    restart: "always"
