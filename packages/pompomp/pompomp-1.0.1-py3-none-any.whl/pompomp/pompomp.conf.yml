# ---------------------------------------------------------------------------
# pompomp.yml
#
# This file defines the core settings for the template management system.
#
# - "packaged" points to the built-in templates directory inside the package.
#   (Do not modify unless you are modifying the internal package structure.)
#
# - "user" will be automatically populated when initializing templates.
#   (Do not edit manually unless you know exactly what you are doing =P)
# ---------------------------------------------------------------------------
templates_root:
  packaged: ./templates
  user:
