[project]
name = "dundle"
version = "2025.5.22"
description = "dundle.py - Convenient, non-standard package builder. No pyproject.toml, only dunders like __version__"
readme = {file = "README", content-type = "text/plain"}
classifiers = ["Topic :: Software Development :: Build Tools"]
license = "0BSD"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"