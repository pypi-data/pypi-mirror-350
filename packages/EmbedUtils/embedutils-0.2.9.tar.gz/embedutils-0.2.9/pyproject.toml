[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "EmbedUtils"
version = "0.2.9"
description = "Library for converting dicts to embeds"
authors = [
    { name="MildlyObesePanda" }
]
license = { text = "MIT" }
dependencies = ["requests", "discord", "asyncio"]

[project.urls]
Homepage = "https://github.com/krexxy/embedutils"
