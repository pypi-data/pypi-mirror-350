[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "EmbedUtils"
version = "0.1.2"
description = "Discord embed class utilities"
authors = [
    { name="KrexBex" }
]
license = { text = "MIT" }
dependencies = ["requests", "discord", "asyncio"]

[project.urls]
Homepage = "https://github.com/krexxy/embedutils"
