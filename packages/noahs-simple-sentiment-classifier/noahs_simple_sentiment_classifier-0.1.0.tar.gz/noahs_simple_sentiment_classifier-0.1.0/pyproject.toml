[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "noahs_simple_sentiment_classifier"  # Must be unique on PyPI!
version = "0.1.0"
description = "A combination of classification implementations for finding if text is 'friendly', 'hostile', or 'neutral'"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
  { name = "Noah Jones", email = "jonesnoah45010@gmail.com" }
]
dependencies = [
  "textblob",
]

[project.urls]
"Homepage" = "https://github.com/jonesnoah45010/simple_sentiment_classifier"
