# Sensor901 Driver

## Development

Run unit tests:

```shell
coverage run -m unittest discover tests
```

Generate coverage report:

```shell
# result in console
coverage report -m

# or result in HTML format
coverage html
```