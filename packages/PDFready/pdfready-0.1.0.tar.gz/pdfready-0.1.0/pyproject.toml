[project]
name = "PDFready"
version = "0.1.0"
description = "Un lector de archivos PDF en Python"
authors = [
  { name="Daniel Montañez & Juherles Bravo", email="gus6gus6gutavo6@gmail.com" }
]
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]