[project]
name = "country-server"
version = "1.0.1"
description = "A simple MCP country-lookup server"
requires-python = ">=3.8"
dependencies = [
  "fastapi",
  "uvicorn",
  "sse-starlette"
]

[project.scripts]
country-server = "country_server:cli"
