[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mathify"
version = "0.1.0"
description = "A package for mathematical utilities and functions"
readme = "README.md"
requires-python = ">=3.6"
authors = [
    { name = "David Warner", email = "kaboom31@example.com" }
]
license = { text = "MIT License" }
homepage = "https://github.com/yourusername/mathify"
dependencies = [
    # List your dependencies here, e.g.,
    # "numpy>=1.21"
]

[project.urls]
"Source" = "https://github.com/none/mathify"
