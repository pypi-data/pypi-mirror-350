[project]
name = "volttron-website"
version = "0.1.1a2"
description = "A test repository for github actions"
authors = [
    {name = "C. Allwardt",email = "3979063+craig8@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic (>=2.11.4,<3.0.0)"
]
[tool.poetry.group.dev.dependencies]
pre-commit = "^3.2.2"

[tool.poetry]
packages = [
    { include = "volttron_test_website", from = "src" }
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
