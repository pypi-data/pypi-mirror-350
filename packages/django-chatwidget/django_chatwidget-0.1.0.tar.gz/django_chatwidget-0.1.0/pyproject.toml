[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "django-chatwidget"
version = "0.1.0"
description = "A reusable Django AI chat widget powered by OpenAI and JSON knowledge bases."
authors = [{name = "Matthew Raynor", email = "mnraynor90@gmail.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "django>=3.2",
    "openai>=1.0.0"
]
