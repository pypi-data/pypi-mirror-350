[project]
name = "pandas-query-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "neka-nat", email = "nekanat.stock@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.1",
    "openpyxl>=3.1.5",
    "pandas>=2.2.3",
    "tabulate>=0.9.0",
]

[project.scripts]
pandas-query-mcp = "pandas_query_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
