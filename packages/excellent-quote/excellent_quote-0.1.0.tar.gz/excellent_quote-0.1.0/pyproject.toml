[project]
name = "excellent_quote"
version = "0.1.0"
description = "Tiny CLI that prints random Kevin Kelly quotes"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
dependencies = ["click"]

[project.scripts]
excellent_quote = "excellent_quote.cli:main"

[tool.setuptools.package-data]
excellent_quote = ["quotes.csv"]       
