[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ant_instability_detection"
version = "0.0.1"
description = "Ensure there is not instability in the ant protocol."
authors = [
  { name="Bastien Legras", email="Bastien.Legras@proton.me" }
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
