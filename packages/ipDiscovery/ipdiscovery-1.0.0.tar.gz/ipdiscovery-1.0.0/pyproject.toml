[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ipDiscovery"
version = "1.0.0"
description = "A library to get the OS IP post-deployment from MAC address"
authors = [
    { name = "Vikas Kumar", email = "fusionvikas.py@gmail.com" }
]
readme = "README.md"
license = "MIT"
requires-python = ">=3.7"
dependencies = []

[project.urls]
Homepage = "https://github.com/yourusername/ipDiscovery"
