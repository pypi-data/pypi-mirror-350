[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ipDiscovery"
version = "1.0.3"
description = "A library designed to retrieve the operating system's IP address after deployment using its MAC address"
authors = [
    { name = "Vikas Kumar", email = "fusionvikas.py@gmail.com" }
]
readme = "README.md"
license = "MIT"
requires-python = ">=3.7"
dependencies = []

[project.urls]
Homepage = "https://github.com/fusionvikas2015/ipDiscovery.git"
