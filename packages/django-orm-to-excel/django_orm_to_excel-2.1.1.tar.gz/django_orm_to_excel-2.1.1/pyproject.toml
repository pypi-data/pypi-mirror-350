[project]
name = "django-orm-to-excel"
description = "Turning Django ORM objects to Excel"
requires-python = ">=3.9"
dependencies = []
version = "2.1.1"
readme = "README.md"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[tool.setuptools]
include-package-data = true
