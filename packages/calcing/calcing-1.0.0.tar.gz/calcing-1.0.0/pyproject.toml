[project]
name = "calcing"
version = "1.0.0"
description = "calcing.plus() calcing.minus() calcing.mnoz() calcing.dil() calcing.diln()"
authors = [
    {name = "kasked"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
