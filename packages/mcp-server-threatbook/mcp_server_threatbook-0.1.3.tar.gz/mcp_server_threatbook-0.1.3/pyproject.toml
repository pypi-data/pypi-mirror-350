[project]
name = "mcp-server-threatbook"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wowtalon", email = "1120692265@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.1",
    "requests>=2.32.3",
]

[project.scripts]
mcp-server-threatbook = "mcp_server_threatbook:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
