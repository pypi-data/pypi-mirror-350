[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "andyreagan"
version = "0.1.0"
description = "Andy Reagan's resume printer"
authors = [{name = "Andy Reagan"}]
dependencies = []
requires-python = ">=3.8"

[project.scripts]
andyreagan = "andyreagan.main:main"

[tool.hatch.build.targets.wheel]
packages = ["andyreagan"]