[project]
name = "calc-norm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "urstrulyrohans", email = "urstrulyrohansd@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "setuptools>=80.8.0",
]

[project.scripts]
calc-norm = "calc_norm:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
