[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pygard"
version = "0.1.0"
description = "Python SDK for Gard Data Service"
authors = [
    { name = "qingmu", email = "qingmu_0722@163.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.2.3",
    "pydantic>=2.11.4",
    "requests>=2.32.3",
]

[project.urls]
Repository = "https://code.deep-time.org/QingMu/data-service-sdk-python"
