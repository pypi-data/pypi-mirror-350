[project]
name = "mm-agent-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.9.1",
    "requests>=2.32.3",
]

[tool.poetry.scripts]
hybrid_search = 'main:hybrid_search'

[project.scripts]
mm-agent-mcp = "main:main"
