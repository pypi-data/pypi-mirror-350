[project]
name = "mm-agent-mcp"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.12.0",
    "mcp>=1.9.1",
    "requests>=2.32.3",
]

[tool.poetry.scripts]
hybrid_search = 'main:hybrid_search'

[project.scripts]
mm-agent-mcp = "mm_agent_mcp:main"

[virtualenvs]
in-project = true
