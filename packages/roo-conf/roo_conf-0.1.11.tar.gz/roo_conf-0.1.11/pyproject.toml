[project]
name = "roo-conf"
version = "0.1.11"
description = "A configuration and prompt updater for Roo Code"
readme = "README.md"
authors = [
    { name = "Mark Stouffer", email = "1802850+InTEGr8or@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
exclude = ["*.sh"]

[project.scripts]
roo-conf = "roo_conf:main"
