[project]
name = "linux-hwinfo64"
version = "0.1.3"
description = "A lightweight, terminal-based system monitoring tool for Linux that provides real-time hardware metrics (CPU and GPU) in both text and graphical formats"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "psutil>=7.0.0",
    "ruff>=0.11.10",
]
