from setuptools import setup, find_packages

setup(
    name='ebro_caesarcipher',
    version='0.1.2',
    packages=find_packages(),
    author='Ebro1500',
    description='A simple Python library that implements the Caesar cipher algorithm.',
    install_requires=[],
)
