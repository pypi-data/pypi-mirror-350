[project]
name = "singpass-client"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "khinyu", email = "khinyu2000@gmail.com" }
]
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "joserfc>=1.0.4",
]

[project.scripts]
singpass-client = "singpass_client:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
