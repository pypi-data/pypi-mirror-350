[project]
name = "lzhgetlogger"
version = "1.0"
description = "简化 logging 配置，统一格式"
authors = [
    { name = "lzh", email = "zhhtdm@hotmail.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
]

[project.urls]
Homepage = "https://github.com/zhhtdm/get-logger"
Repository = "https://github.com/zhhtdm/get-logger"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
