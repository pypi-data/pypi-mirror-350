[project]
name = "pyloid-builder"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hyeonho cho", email = "111675679+aesthetics-of-record@users.noreply.github.com" }
]
requires-python = ">=3.9"
dependencies = [
    "pyinstaller>=6.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
