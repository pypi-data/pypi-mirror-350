[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prooheckcp-hello-world"
version = "0.4"
license = {text = "MIT"}
description = "A simple Hello World CLI tool"
authors = [{ name="prooheckcp", email="vasco.soares.2001@gmail.com" }]
readme = "README.md"
requires-python = ">=3.6"

[project.scripts]
prooheckcp-hello-world = "prooheckcp_hello_world:main"