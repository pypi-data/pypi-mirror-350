[project]
name = "statcat"
version = "0.0.0"
description = "A small Python library of statistics functions."
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [{ name = "Caleb", email = "136144800+cracker76239@users.noreply.github.com" }]
keywords = ["statistics", "math"]
requires-python = ">=3.10"

[project.scripts]
my_package_cli = "statcat.statcat:main"  # adjust if needed

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"

