[project]
name = "auto-smart-commit"
version = "0.1.0"
description = ""
authors = [
    {name = "SilverPeak2299",email = "dannydickerson03@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click (>=8.2.1,<9.0.0)",
    "keyring (>=25.6.0,<26.0.0)",
    "google-genai (>=1.16.1,<2.0.0)",
]

[tool.poetry]
packages = [{include = "smart_commit", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[project.scripts]
smart-commit = "smart_commit.cli:main"
smart-commit-config = "smart_commit.config:main"
