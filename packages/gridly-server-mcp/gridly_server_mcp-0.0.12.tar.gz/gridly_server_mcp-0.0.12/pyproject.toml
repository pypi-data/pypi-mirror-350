[project]
name = "gridly-server-mcp"
version = "0.0.12"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.0",
    "requests>=2.31.0"
]

[project.scripts]
gridly-server-mcp = "app.server:main"

