[project]
name = "controlresell-connector"
version = "0.2.22"
description = "Auto-generated Pydantic project from Kotlin using guimauvedigital/zodable"
dependencies = [ "pydantic", "zodable-idschema", "uuid", "typing", "datetime",]
requires-python = ">=3.9"

[build-system]
requires = [ "setuptools", "wheel",]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-data]
controlresell_connector = [ "py.typed",]
