[project]
name = "web_fractal"
version = "0.0.1"
requires-python = ">=3.12"
authors = [
    { name = "Aleksandr", email = "vkusnisaharok@gmail.com" },
]
description = "reborn to kill"

[build-system]
requires = ["uv>=0.1.0", "setuptools>=65.0.0"]
build-backend = "setuptools.build_meta"