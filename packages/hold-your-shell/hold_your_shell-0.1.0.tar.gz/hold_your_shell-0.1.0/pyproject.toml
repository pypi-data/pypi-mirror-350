[project]
name = "hold-your-shell"
version = "0.1.0"
description = "Preview and confirm scripts before executing them."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
hold-your-shell = "hold_your_shell:hold_your_shell.run"

