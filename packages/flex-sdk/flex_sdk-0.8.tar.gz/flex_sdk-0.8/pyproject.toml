[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flex-sdk"
version = "0.8"
description = "Python SDK for Flex API"
requires-python = ">=3.7"
dependencies = [
    "requests",
]

[tool.setuptools]
package-dir = {"" = "src"} 