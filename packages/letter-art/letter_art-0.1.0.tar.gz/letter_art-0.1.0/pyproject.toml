[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "letter-art"                   # 패키지명
version = "0.1.0"
description = "Convert images to ASCII letter art"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
    {name = "Your Name", email = "you@example.com"}
]
dependencies = [
    "pillow>=9.0.0"
]

[project.urls]
"Homepage" = "https://github.com/gwondoo/letter_art"
