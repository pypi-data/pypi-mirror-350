{
  "name": "divine-agent",
  "private": true,
  "scripts": {
    "prebuild": "turbo prebuild --cache=local:r,remote:r",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "analyze": "turbo analyze",
    "bump-deps": "npx npm-check-updates --deep -u -x react-day-picker && pnpm install"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@workspace/typescript-config": "workspace:*",
    "turbo": "^2.5.2",
    "typescript": "5.7.3",
    "ultracite": "^4.2.1"
  },
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=18"
  }
}
