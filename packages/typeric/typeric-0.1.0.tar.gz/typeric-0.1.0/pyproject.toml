[project]
name = "typeric"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dfine", email = "dfine@nnkosmos.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
typeric = "typeric:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
