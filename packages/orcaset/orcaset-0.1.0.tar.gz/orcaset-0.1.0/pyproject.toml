[project]
name = "orcaset"
version = "0.1.0"
description = "Model toolkit"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
