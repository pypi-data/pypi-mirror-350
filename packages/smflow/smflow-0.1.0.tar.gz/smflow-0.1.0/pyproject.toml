[project]
name = "smflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "W. Meijer", email = "houterm@gmail.com" }]
requires-python = ">=3.10"
dependencies = [
    "gitpython>=3.1.44",
    "uv>=0.7.8",
]

[project.scripts]
smflow = "smflow:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
