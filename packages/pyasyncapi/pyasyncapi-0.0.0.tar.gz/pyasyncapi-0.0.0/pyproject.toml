[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[project]
name = "pyasyncapi"
version = "0.0.0"
description = "Declarative AsyncAPI schema generation for Python"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.11.5",
    "pyyaml>=6.0.2",
]

[tool]
